-- ================================================================
-- sites テーブル — 旧RLSポリシー削除
-- ================================================================
-- 削除対象: 日本語名の古いポリシー3件（英語名の新ポリシーに統合済み）
-- ================================================================
-- Supabase ダッシュボード > SQL Editor で実行してください
-- ================================================================

DROP POLICY IF EXISTS "自社現場のみ参照" ON sites;
DROP POLICY IF EXISTS "自社現場のみ登録" ON sites;
DROP POLICY IF EXISTS "自社現場のみ更新" ON sites;

-- ----------------------------------------------------------------
-- 確認クエリ（実行後に残っているポリシーを確認）
-- ----------------------------------------------------------------
-- SELECT policyname, cmd
-- FROM pg_policies
-- WHERE tablename = 'sites'
-- ORDER BY cmd;
